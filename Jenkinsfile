pipeline {  
    agent any  
        stages {  
       	   //Code starts for stage Cleaning
        stage('Clean') {
            steps {
                sh 'mvn clean'
            }
        }
		//Code ends for stage Clean
        }
}
